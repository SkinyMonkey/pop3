use mlua::prelude::*;

/// Register all PopScript constants as Lua globals.
///
/// This is a placeholder -- full constants will be added in Task 2.
pub fn register_constants(_lua: &Lua) -> LuaResult<()> {
    Ok(())
}
