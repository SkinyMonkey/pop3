---
phase: 05-ai-and-campaign
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/engine/ai/target.rs
  - src/engine/ai/building_ai.rs
  - src/engine/ai/shaman_cmd.rs
  - src/engine/ai/difficulty.rs
autonomous: true
requirements: [AI-04, AI-05, AI-06, AI-07]

must_haves:
  truths:
    - "Target scoring matches original binary weights (Shaman=1000, Super Warrior=20, etc.)"
    - "AI building placement validates terrain and follows priority order"
    - "Shaman command queue dispatches 8 command types with 10 slots per tribe"
    - "Difficulty scaling applies separate mana/training costs for AI vs human"
  artifacts:
    - path: "src/engine/ai/target.rs"
      provides: "Target scoring algorithm matching original binary"
      exports: ["score_target", "TargetScore"]
    - path: "src/engine/ai/building_ai.rs"
      provides: "AI building placement state machine"
      exports: ["AiBuildingPlacement"]
    - path: "src/engine/ai/shaman_cmd.rs"
      provides: "Shaman command queue and dispatch"
      exports: ["ShamanCommandQueue", "ShamanCommandType"]
    - path: "src/engine/ai/difficulty.rs"
      provides: "Difficulty scaling constants and application"
      exports: ["DifficultyScaling", "apply_mana_adjust"]
  key_links:
    - from: "src/engine/ai/target.rs"
      to: "docs/specs/ai_scripting.md"
      via: "Score values from AI_FindBestAttackTarget"
      pattern: "SHAMAN.*1000|SUPER_WARRIOR.*20"
    - from: "src/engine/ai/difficulty.rs"
      to: "AI training costs"
      via: "COMPUTER_MANA_ADJUST and CP_TRAIN_MANA constants"
      pattern: "COMPUTER_MANA_ADJUST"
---

<objective>
Implement AI behavior modules: target selection scoring, building placement AI, shaman command system, and difficulty scaling. All using faithful constants from the original binary.

Purpose: These modules provide the decision-making intelligence that AI tribe scripts call through PopScript functions.
Output: Four AI behavior modules implementing target scoring, building AI, shaman commands, and difficulty scaling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ai-and-campaign/05-CONTEXT.md
@.planning/phases/05-ai-and-campaign/05-RESEARCH.md
@.planning/phases/05-ai-and-campaign/05-01-SUMMARY.md
@docs/specs/ai_scripting.md
</context>

<interfaces>
<!-- From docs/specs/ai_scripting.md - Target Scoring -->
```
Person scoring:
  SHAMAN: 1000, SUPER_WARRIOR: 20, WARRIOR: 10, PREACHER: 15, SPY: 5
  Exposed shaman bonus: +500 (nearbyDefenders < 3)
Building scoring:
  SUPER_WARRIOR_TRAINING: 250, TEMPLE: 200, WARRIOR_TRAINING: 180, DRUM_TOWER: 150, default: 50
Distance penalty: score -= distance_from_base / 100

Threat weights:
  Shaman: 50, Super Warrior: 20, Preacher: 15, Warrior: 10, Spy: 5

Shaman command types (8):
  0=PrimaryAttack, 1=SecondaryAttack, 2=DefendPosition, 3=SpellCasting,
  4=ArmyMovement, 5=BuildingPlacement, 6=ResourceGathering, 7=Conversion
  10 slots per tribe at offset +0x74, each 0x52 bytes

Difficulty:
  COMPUTER_MANA_ADJUST: multiplier/100 on mana rate
  CP_TRAIN_MANA_WARR/SPY/PREACH/SWARR: separate training costs
  Training cost bands: 0-3=100%, 4-7=120%, 8-11=140%, 12-15=160%, 16-20=180%, 21+=200%

Building priority: Drum Towers > Training > Housing > Reincarnation
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Target scoring and threat assessment</name>
  <files>src/engine/ai/target.rs, src/engine/ai/mod.rs</files>
  <read_first>
    - src/engine/ai/mod.rs
    - docs/specs/ai_scripting.md
  </read_first>
  <behavior>
    - Test: score_person_target(SHAMAN, 0, 100) returns 1000 - 100/100 = 999
    - Test: score_person_target(SHAMAN, 2, 0) returns 1000 + 500 (exposed bonus: defenders < 3)
    - Test: score_person_target(WARRIOR, 5, 200) returns 10 - 200/100 = 8
    - Test: score_building_target(SUPER_TRAIN, 0) returns 250
    - Test: score_building_target(DRUM_TOWER, 300) returns 150 - 3 = 147
    - Test: assess_threat with 2 warriors + 1 shaman = 2*10 + 50 = 70
    - Test: score_person_target(SPY, 10, 500) returns 5 - 5 = 0 (min 0)
  </behavior>
  <action>
1. Add `pub mod target;` and `pub mod building_ai;` and `pub mod shaman_cmd;` and `pub mod difficulty;` to src/engine/ai/mod.rs

2. Create src/engine/ai/target.rs:

   ```rust
   /// Target scoring constants matching AI_FindBestAttackTarget @ 0x004b9770
   pub const SCORE_SHAMAN: i32 = 1000;
   pub const SCORE_SUPER_WARRIOR: i32 = 20;
   pub const SCORE_PREACHER: i32 = 15;
   pub const SCORE_WARRIOR: i32 = 10;
   pub const SCORE_SPY: i32 = 5;
   pub const SCORE_EXPOSED_SHAMAN_BONUS: i32 = 500;
   pub const EXPOSED_SHAMAN_DEFENDER_THRESHOLD: u32 = 3;

   pub const SCORE_BUILDING_SUPER_TRAIN: i32 = 250;
   pub const SCORE_BUILDING_TEMPLE: i32 = 200;
   pub const SCORE_BUILDING_WARRIOR_TRAIN: i32 = 180;
   pub const SCORE_BUILDING_DRUM_TOWER: i32 = 150;
   pub const SCORE_BUILDING_DEFAULT: i32 = 50;

   pub const DISTANCE_PENALTY_DIVISOR: i32 = 100;

   /// Threat assessment weights matching AI_AssessThreat @ 0x0041ba40
   pub const THREAT_SHAMAN: i32 = 50;
   pub const THREAT_SUPER_WARRIOR: i32 = 20;
   pub const THREAT_PREACHER: i32 = 15;
   pub const THREAT_WARRIOR: i32 = 10;
   pub const THREAT_SPY: i32 = 5;

   /// Person subtypes matching original binary constants
   pub const SUBTYPE_BRAVE: u8 = 2;
   pub const SUBTYPE_WARRIOR: u8 = 3;
   pub const SUBTYPE_PREACHER: u8 = 4;
   pub const SUBTYPE_SPY: u8 = 5;
   pub const SUBTYPE_SUPER_WARRIOR: u8 = 6;
   pub const SUBTYPE_SHAMAN: u8 = 7;

   /// Building subtypes
   pub const BLDG_DRUM_TOWER: u8 = 4;
   pub const BLDG_TEMPLE: u8 = 5;
   pub const BLDG_SPY_TRAIN: u8 = 6;
   pub const BLDG_WARRIOR_TRAIN: u8 = 7;
   pub const BLDG_SUPER_TRAIN: u8 = 8;

   pub fn score_person_target(subtype: u8, nearby_defenders: u32, distance: i32) -> i32 {
       let base = match subtype {
           SUBTYPE_SHAMAN => SCORE_SHAMAN,
           SUBTYPE_SUPER_WARRIOR => SCORE_SUPER_WARRIOR,
           SUBTYPE_PREACHER => SCORE_PREACHER,
           SUBTYPE_WARRIOR => SCORE_WARRIOR,
           SUBTYPE_SPY => SCORE_SPY,
           _ => 0,
       };
       let bonus = if subtype == SUBTYPE_SHAMAN && nearby_defenders < EXPOSED_SHAMAN_DEFENDER_THRESHOLD {
           SCORE_EXPOSED_SHAMAN_BONUS
       } else {
           0
       };
       (base + bonus - distance / DISTANCE_PENALTY_DIVISOR).max(0)
   }

   pub fn score_building_target(building_subtype: u8, distance: i32) -> i32 {
       let base = match building_subtype {
           BLDG_SUPER_TRAIN => SCORE_BUILDING_SUPER_TRAIN,
           BLDG_TEMPLE => SCORE_BUILDING_TEMPLE,
           BLDG_WARRIOR_TRAIN => SCORE_BUILDING_WARRIOR_TRAIN,
           BLDG_DRUM_TOWER => SCORE_BUILDING_DRUM_TOWER,
           _ => SCORE_BUILDING_DEFAULT,
       };
       (base - distance / DISTANCE_PENALTY_DIVISOR).max(0)
   }

   pub fn assess_threat(unit_counts: &[(u8, u32)]) -> i32 {
       unit_counts.iter().map(|(subtype, count)| {
           let weight = match *subtype {
               SUBTYPE_SHAMAN => THREAT_SHAMAN,
               SUBTYPE_SUPER_WARRIOR => THREAT_SUPER_WARRIOR,
               SUBTYPE_PREACHER => THREAT_PREACHER,
               SUBTYPE_WARRIOR => THREAT_WARRIOR,
               SUBTYPE_SPY => THREAT_SPY,
               _ => 0,
           };
           weight * (*count as i32)
       }).sum()
   }
   ```

Per D-09: Faithful constants from the original binary.
  </action>
  <verify>
    <automated>cd /Users/adrien/Development/rust/pop3 && cargo test engine::ai::target::tests -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/ai/target.rs contains `SCORE_SHAMAN: i32 = 1000`
    - src/engine/ai/target.rs contains `SCORE_EXPOSED_SHAMAN_BONUS: i32 = 500`
    - src/engine/ai/target.rs contains `pub fn score_person_target`
    - src/engine/ai/target.rs contains `pub fn score_building_target`
    - src/engine/ai/target.rs contains `pub fn assess_threat`
    - src/engine/ai/target.rs contains `THREAT_SHAMAN: i32 = 50`
    - cargo test engine::ai::target::tests passes
  </acceptance_criteria>
  <done>Target scoring matches original binary weights, threat assessment uses documented threat weights</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shaman commands, building AI, and difficulty scaling</name>
  <files>src/engine/ai/shaman_cmd.rs, src/engine/ai/building_ai.rs, src/engine/ai/difficulty.rs</files>
  <read_first>
    - src/engine/ai/mod.rs
    - src/engine/ai/target.rs
    - docs/specs/ai_scripting.md
  </read_first>
  <behavior>
    - Test: ShamanCommandQueue::new() has 10 empty slots
    - Test: push_command adds to first available slot, returns true
    - Test: push_command on full queue returns false
    - Test: clear_command frees slot for reuse
    - Test: command type dispatch returns correct handler name for all 8 types
    - Test: AiBuildingPriority ordering: DrumTower > Training > Housing > Reincarnation
    - Test: apply_mana_adjust(1000, 150) returns 1500 (150% adjustment)
    - Test: apply_mana_adjust(1000, 100) returns 1000 (no change)
    - Test: training_cost_band(3) returns 100 (band 0-3)
    - Test: training_cost_band(5) returns 120 (band 4-7)
    - Test: training_cost_band(21) returns 200 (band 21+)
  </behavior>
  <action>
1. Create src/engine/ai/shaman_cmd.rs:
   ```rust
   pub const MAX_SHAMAN_COMMANDS: usize = 10;
   pub const SHAMAN_COMMAND_SIZE: usize = 0x52;

   #[derive(Debug, Clone, Copy, PartialEq, Eq)]
   pub enum ShamanCommandType {
       PrimaryAttack = 0,
       SecondaryAttack = 1,
       DefendPosition = 2,
       SpellCasting = 3,
       ArmyMovement = 4,
       BuildingPlacement = 5,
       ResourceGathering = 6,
       Conversion = 7,
   }

   impl ShamanCommandType {
       pub fn from_raw(v: u8) -> Option<Self> {
           match v {
               0 => Some(Self::PrimaryAttack),
               1 => Some(Self::SecondaryAttack),
               2 => Some(Self::DefendPosition),
               3 => Some(Self::SpellCasting),
               4 => Some(Self::ArmyMovement),
               5 => Some(Self::BuildingPlacement),
               6 => Some(Self::ResourceGathering),
               7 => Some(Self::Conversion),
               _ => None,
           }
       }
   }

   #[derive(Debug, Clone)]
   pub struct ShamanCommand {
       pub active: bool,
       pub cmd_type: ShamanCommandType,
       pub target_x: u16,
       pub target_y: u16,
       pub target_id: u16,
       pub state: u16,
   }

   impl ShamanCommand {
       pub fn empty() -> Self {
           Self { active: false, cmd_type: ShamanCommandType::PrimaryAttack, target_x: 0, target_y: 0, target_id: 0, state: 0 }
       }
   }

   pub struct ShamanCommandQueue {
       pub slots: [ShamanCommand; MAX_SHAMAN_COMMANDS],
   }

   impl ShamanCommandQueue {
       pub fn new() -> Self {
           Self { slots: std::array::from_fn(|_| ShamanCommand::empty()) }
       }

       pub fn push_command(&mut self, cmd: ShamanCommand) -> bool {
           for slot in &mut self.slots {
               if !slot.active {
                   *slot = cmd;
                   slot.active = true;
                   return true;
               }
           }
           false
       }

       pub fn clear_command(&mut self, index: usize) {
           if index < MAX_SHAMAN_COMMANDS {
               self.slots[index].active = false;
           }
       }

       pub fn active_count(&self) -> usize {
           self.slots.iter().filter(|s| s.active).count()
       }
   }
   ```

2. Create src/engine/ai/building_ai.rs:
   ```rust
   #[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
   pub enum AiBuildingPriority {
       DrumTower = 0,      // Highest (defense)
       Training = 1,       // Military
       Housing = 2,        // Population
       Reincarnation = 3,  // Shaman respawn (lowest)
   }

   /// 7-state placement machine for AI building placement.
   /// Original: AI_ValidateBuildingPlacements + AI_ExecuteBuildingPriorities
   #[derive(Debug, Clone, Copy, PartialEq, Eq)]
   pub enum AiPlacementState {
       Idle,
       SelectType,
       FindLocation,
       ValidateTerrain,
       CheckResources,
       PlaceBuilding,
       WaitConstruction,
   }

   pub struct AiBuildingPlacement {
       pub state: AiPlacementState,
       pub selected_type: Option<u8>,
       pub target_x: i32,
       pub target_y: i32,
       pub priority_queue: Vec<(AiBuildingPriority, u8)>, // (priority, building_type)
   }

   impl AiBuildingPlacement {
       pub fn new() -> Self {
           Self {
               state: AiPlacementState::Idle,
               selected_type: None,
               target_x: 0,
               target_y: 0,
               priority_queue: Vec::new(),
           }
       }

       /// Queue a building for placement, sorted by priority.
       pub fn queue_building(&mut self, building_type: u8) {
           let priority = Self::priority_for_type(building_type);
           self.priority_queue.push((priority, building_type));
           self.priority_queue.sort_by_key(|(p, _)| *p);
       }

       fn priority_for_type(building_type: u8) -> AiBuildingPriority {
           match building_type {
               4 => AiBuildingPriority::DrumTower,      // DRUM_TOWER
               6 | 7 | 8 => AiBuildingPriority::Training, // SPY/WARRIOR/SUPER train
               1 | 2 | 3 => AiBuildingPriority::Housing,  // SMALL/MED/LARGE HUT
               5 => AiBuildingPriority::Reincarnation,     // TEMPLE
               _ => AiBuildingPriority::Housing,
           }
       }
   }
   ```

3. Create src/engine/ai/difficulty.rs:
   ```rust
   /// Difficulty scaling constants from original binary.
   /// Original: COMPUTER_MANA_ADJUST @ 0x005aa8b9

   pub const DEFAULT_MANA_ADJUST: u32 = 100; // 100% = no change

   /// Apply mana adjustment for computer tribes.
   /// adjusted_mana = base_mana * adjust / 100
   pub fn apply_mana_adjust(base_mana: u32, mana_adjust: u32) -> u32 {
       (base_mana as u64 * mana_adjust as u64 / 100) as u32
   }

   /// Training cost bands: diminishing returns as unit count increases.
   /// Original binary uses 6 bands with increasing multipliers.
   pub fn training_cost_band(unit_count: u32) -> u32 {
       match unit_count {
           0..=3 => 100,
           4..=7 => 120,
           8..=11 => 140,
           12..=15 => 160,
           16..=20 => 180,
           _ => 200,
       }
   }

   /// Apply training cost with band multiplier.
   /// base_cost * band_multiplier / 100
   pub fn adjusted_training_cost(base_cost: u32, current_count: u32) -> u32 {
       base_cost * training_cost_band(current_count) / 100
   }

   /// Per-unit-type AI training mana costs (separate from human costs).
   /// Original: CP_TRAIN_MANA_* constants
   pub struct AiTrainingCosts {
       pub warrior: u32,
       pub spy: u32,
       pub preacher: u32,
       pub super_warrior: u32,
   }

   impl AiTrainingCosts {
       pub fn default_costs() -> Self {
           Self {
               warrior: 3000,
               spy: 5000,
               preacher: 8000,
               super_warrior: 12000,
           }
       }
   }

   pub struct DifficultyScaling {
       pub mana_adjust: u32,
       pub training_costs: AiTrainingCosts,
   }

   impl DifficultyScaling {
       pub fn normal() -> Self {
           Self {
               mana_adjust: DEFAULT_MANA_ADJUST,
               training_costs: AiTrainingCosts::default_costs(),
           }
       }
   }
   ```

Per D-09: Faithful constants from the original binary (target scoring weights, threat assessment, building priorities, training cost bands).
  </action>
  <verify>
    <automated>cd /Users/adrien/Development/rust/pop3 && cargo test engine::ai::shaman_cmd::tests engine::ai::building_ai::tests engine::ai::difficulty::tests -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/ai/shaman_cmd.rs contains `pub const MAX_SHAMAN_COMMANDS: usize = 10`
    - src/engine/ai/shaman_cmd.rs contains `pub enum ShamanCommandType` with 8 variants
    - src/engine/ai/shaman_cmd.rs contains `pub fn push_command`
    - src/engine/ai/building_ai.rs contains `pub enum AiBuildingPriority`
    - src/engine/ai/building_ai.rs contains `pub enum AiPlacementState` with 7 variants
    - src/engine/ai/difficulty.rs contains `pub fn apply_mana_adjust`
    - src/engine/ai/difficulty.rs contains `pub fn training_cost_band`
    - src/engine/ai/difficulty.rs contains `DEFAULT_MANA_ADJUST: u32 = 100`
    - cargo test passes for all three modules
  </acceptance_criteria>
  <done>Shaman command queue dispatches 8 types with 10 slots, building AI follows priority order, difficulty scaling applies mana adjust and training cost bands</done>
</task>

</tasks>

<verification>
- `cargo test engine::ai` passes all AI behavior tests
- Target scoring matches documented binary values exactly
- All 8 shaman command types implemented
- Training cost bands produce correct multipliers
</verification>

<success_criteria>
- Target scoring: Shaman=1000, exposed bonus=+500, distance penalty works
- Shaman commands: 10 slots, push/clear/count, all 8 types
- Building AI: 7-state machine, priority ordering matches original
- Difficulty: mana adjust multiplier, 6 training cost bands
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-and-campaign/05-05-SUMMARY.md`
</output>
