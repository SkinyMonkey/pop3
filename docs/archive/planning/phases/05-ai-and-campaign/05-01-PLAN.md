---
phase: 05-ai-and-campaign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/engine/mod.rs
  - src/engine/ai/mod.rs
  - src/engine/ai/constants.rs
autonomous: true
requirements: [AI-01, AI-03]

must_haves:
  truths:
    - "mlua Lua 5.4 VM can be created and execute Lua code"
    - "All 3469 PopScript constants (INT_*, ATTR_*, Defines) are available as Lua globals"
    - "AI module exists under src/engine/ai/ with public exports"
  artifacts:
    - path: "Cargo.toml"
      provides: "mlua, serde, bincode dependencies"
      contains: "mlua"
    - path: "src/engine/ai/mod.rs"
      provides: "AiSystem struct, module declarations"
      exports: ["AiSystem"]
    - path: "src/engine/ai/constants.rs"
      provides: "PopScript constant registration into Lua VM"
      contains: "register_constants"
  key_links:
    - from: "src/engine/ai/mod.rs"
      to: "mlua::Lua"
      via: "AiSystem owns Lua VM instance"
      pattern: "mlua::Lua"
    - from: "src/engine/ai/constants.rs"
      to: "Lua globals"
      via: "register_constants sets globals on Lua VM"
      pattern: "globals\\.set"
---

<objective>
Add mlua/serde/bincode dependencies to Cargo.toml and create the AI module skeleton with a Lua 5.4 VM that has all 3469 PopScript constants registered as globals.

Purpose: Foundation for AI scripting -- the Lua VM with constants is needed before PopScript functions or script loading can work.
Output: Compiling project with mlua, AiSystem struct that creates a Lua VM, constants module with all INT_*/ATTR_*/Defines values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-and-campaign/05-CONTEXT.md
@.planning/phases/05-ai-and-campaign/05-RESEARCH.md
@docs/specs/ai_scripting.md
</context>

<interfaces>
<!-- From src/engine/state/traits.rs -->
```rust
pub trait AiTick {
    fn tick_update_ai(&mut self);
}
pub struct NoOp;
impl AiTick for NoOp {
    fn tick_update_ai(&mut self) {}
}
```

<!-- From src/engine/mod.rs -->
```rust
pub mod command;
pub mod economy;
pub mod frame;
pub mod state;
pub mod movement;
pub mod objects;
pub mod terrain;
pub mod units;
pub mod buildings;
pub mod combat;
pub mod effects;
```

<!-- From src/engine/state/tick.rs -->
```rust
pub struct GameWorld {
    pub state: GameState,
    pub flags: GameFlags,
    pub rng: GameRng,
    pub tribes: TribeArray,
    pub game_speed: u32,
    pub game_tick: u32,
    pub tick_counter: u32,
    pub ai_update_mult: i8,
    pub player_tribe: u8,
    pub tutorial_mode: u8,
    // ...
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies and create AI module skeleton with Lua VM</name>
  <files>Cargo.toml, src/engine/mod.rs, src/engine/ai/mod.rs</files>
  <read_first>
    - Cargo.toml
    - src/engine/mod.rs
    - src/engine/state/traits.rs
    - src/engine/state/tick.rs
  </read_first>
  <behavior>
    - Test: AiSystem::new() succeeds without panic
    - Test: AiSystem's Lua VM can execute simple Lua code (lua.load("return 1+1").eval::<i32>() == 2)
    - Test: AiSystem implements AiTick trait (tick_update_ai callable)
  </behavior>
  <action>
1. Add to Cargo.toml [dependencies]:
   mlua = { version = "0.11", features = ["lua54", "vendored"] }
   serde = { version = "1", features = ["derive"] }
   bincode = "2"

2. Add `pub mod ai;` to src/engine/mod.rs (after `pub mod effects;`)

3. Create src/engine/ai/mod.rs:
   - `pub mod constants;` declaration
   - `use mlua::prelude::*;`
   - `pub struct TribeScriptState` with fields: variables (Vec<i32> of 64 entries), every_counters (HashMap<String, u32>), active (bool)
   - `pub struct AiSystem` with fields: lua (Lua), tribe_states ([TribeScriptState; 4])
   - `impl AiSystem`:
     - `pub fn new() -> LuaResult<Self>`: creates Lua::new(), calls constants::register_constants(&lua), initializes 4 TribeScriptStates
     - `pub fn is_player(&self, tribe_idx: usize, player_tribe: u8) -> bool`: tribe_idx == player_tribe as usize
   - `impl AiTick for AiSystem`:
     - `fn tick_update_ai(&mut self) {}` -- empty stub, wired in plan 06

Per D-01: Use mlua (Rust bindings to Lua 5.4).
Per D-10: One shared Lua VM instance (Claude's discretion: shared VM with per-tribe state tables).
  </action>
  <verify>
    <automated>cd /Users/adrien/Development/rust/pop3 && cargo test engine::ai::tests -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `mlua = { version = "0.11", features = ["lua54", "vendored"] }`
    - Cargo.toml contains `serde = { version = "1", features = ["derive"] }`
    - Cargo.toml contains `bincode = "2"`
    - src/engine/mod.rs contains `pub mod ai;`
    - src/engine/ai/mod.rs contains `pub struct AiSystem`
    - src/engine/ai/mod.rs contains `impl AiTick for AiSystem`
    - src/engine/ai/mod.rs contains `pub mod constants;`
    - cargo test engine::ai::tests passes
  </acceptance_criteria>
  <done>AiSystem struct exists, creates a Lua 5.4 VM, implements AiTick trait, cargo build succeeds with mlua/serde/bincode deps</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register all 3469 PopScript constants into Lua globals</name>
  <files>src/engine/ai/constants.rs</files>
  <read_first>
    - src/engine/ai/mod.rs
    - docs/specs/ai_scripting.md
  </read_first>
  <behavior>
    - Test: After register_constants(), Lua globals contain INT_MY_NUM_PEOPLE (expected value from PopScript spec)
    - Test: After register_constants(), Lua globals contain ATTR_EXPANSION (attribute constant)
    - Test: After register_constants(), Lua globals contain INT_BLUE (tribe constant = 0)
    - Test: After register_constants(), Lua globals contain known Defines constants like ATTACK_NORMAL
    - Test: Total number of registered constants >= 3469
  </behavior>
  <action>
Create src/engine/ai/constants.rs with:

1. `pub fn register_constants(lua: &mlua::Lua) -> mlua::Result<()>` that registers all PopScript constants as Lua globals.

2. Constants come from three sources (per D-03):
   a. Script4_Popscript module: 168 function names (registered as stubs here, replaced with real implementations in plan 04) + 299 constants
   b. Script4_Defines module: 3170 constants

3. The constant values and names must be derived from the PopScript Wiki documentation and the script4_system_spec.json structure. Key constant groups:

   **Tribe constants:** INT_BLUE=0, INT_RED=1, INT_YELLOW=2, INT_GREEN=3
   **Spell type constants (from ai_scripting.md codes 1184-1198):**
   INT_BURN=1, INT_BLAST=2, INT_LIGHTNING=3, INT_TORNADO=4, INT_SWARM=5, INT_INVISIBILITY=6, INT_HYPNOTISM=7, INT_FIRESTORM=8, INT_GHOST_ARMY=9, INT_EROSION=10, INT_SWAMP=11, INT_LAND_BRIDGE=12, INT_ANGEL_OF_DEATH=13, INT_EARTHQUAKE=14, INT_FLATTEN=15, INT_VOLCANO=16, INT_CONVERT=17, INT_ARMAGEDDON=18, INT_SHIELD=19, INT_BLOODLUST=20, INT_TELEPORT=21
   **Unit type constants (from codes 1201-1206):** INT_BRAVE=2, INT_WARRIOR=3, INT_PREACHER=4, INT_SPY=5, INT_SUPER_WARRIOR=6, INT_SHAMAN=7
   **Building type constants (from codes 1207-1214):** INT_SMALL_HUT=1, INT_MEDIUM_HUT=2, INT_LARGE_HUT=3, INT_DRUM_TOWER=4, INT_TEMPLE=5, INT_SPY_TRAIN=6, INT_WARRIOR_TRAIN=7, INT_SUPER_TRAIN=8, INT_BOAT_HUT=13, INT_AIRSHIP_HUT=14, INT_GUARD_TOWER=15
   **Attack type constants:** ATTACK_NORMAL=0, ATTACK_BY_BOAT=1, ATTACK_BY_BALLOON=2
   **Marker constants:** INT_M_PERSON_LEFT_OF_DRUM_TOWER=3, INT_M_PERSON_RIGHT_OF_DRUM_TOWER=4, etc. (marker IDs 0-255)
   **Internal attribute constants (codes 1000-1237 from ai_scripting.md):** These are used as INT_* reads in PopScript. They map to game state queries. Register as integer constants here; the actual game state query happens via PopScript functions in plan 04.

4. Register stub functions for the 168 PopScript commands. Each stub should call `panic!("PopScript function '{}' not yet implemented", name)` per D-04. These stubs will be replaced with real implementations in plan 04. Use a helper function:
   ```rust
   fn register_stub(lua: &Lua, globals: &Table, name: &str) -> LuaResult<()> {
       let name_owned = name.to_string();
       globals.set(name, lua.create_function(move |_, args: mlua::MultiValue| -> LuaResult<i32> {
           panic!("PopScript function '{}' not yet implemented (called with {} args)", name_owned, args.len());
       })?)?;
       Ok(())
   }
   ```

5. PopScript function names to register as stubs (168 total, key ones):
   ATTACK, ATTACK_MARKER, BUILD_AT, BOAT_PATROL, CAMERA_ROTATION, CLEAR_HOUSE_INFO_FLAG, CLEAR_STANDING_PEOPLE, CONVERT_AT_MARKER, COUNT_PEOPLE_IN_HOUSES, DEFEND_SHAMEN, DELETE_SMOKE_STUFF, DESELECT_ALL_PEOPLE, DONT_TARGET_SHAMAN, EVERY, FLASH_BUTTON, FIX_WILD_IN_AREA, FLYBY_CREATE_NEW, FLYBY_SET_EVENT_POS, FLYBY_SET_EVENT_ANGLE, FLYBY_SET_EVENT_ZOOM, FLYBY_SET_EVENT_INT_POINT, FLYBY_SET_EVENT_TOOLTIP, FLYBY_SET_END_TARGET, FLYBY_START, FLYBY_STOP, FLYBY_ALLOW_INTERRUPT, GET_HEAD_TRIGGER_COUNT, GET_HEIGHT_AT_POS, GIVE_MANA_TO_PLAYER, HAS_TIMER_REACHED_ZERO, I_KILL_CONVERTABLE, IS_BUILDING_NEAR, IS_PRISONER_LEFT, IS_SHAMAN_AVAILABLE_FOR_ATTACK, IS_SHAMAN_IN_AREA, MARKER_ENTRIES, MARVELLOUS_HOUSE_DEATH, NAV_CHECK, ONLY_STAND_AT_MARKERS, OPEN_DIALOG, PARTIAL_BUILDING_COUNT, PRAY_AT_HEAD, PREACH_AT_MARKER, REMOVE_HEAD_AT_POS, REMOVE_PLAYER_THING, REMOVE_TIMER, RESET_BASE_MARKER, SEND_ALL_PEOPLE_TO_MARKER, SEND_BLUE_PEOPLE_TO_MARKER, SEND_GHOSTS_TO_MARKER, SEND_GREEN_PEOPLE_TO_MARKER, SEND_PEOPLE_TO_MARKER, SEND_RED_PEOPLE_TO_MARKER, SEND_SHAMAN_DEFENDERS_HOME, SET_ATTACK_VARIABLE, SET_BASE_MARKER, SET_BASE_RADIUS, SET_BOAT_HOUSE_WITH_BOAT, SET_BUCKET_USAGE, SET_BUILDING_DIRECTION, SET_DEFENCE_RADIUS, SET_DRUM_TOWER_POS, SET_MARKER_ENTRY, SET_MSG_AUTO_OPEN_DLG, SET_MSG_DELETE_ON_OK, SET_MSG_ID, SET_MSG_NARRATIVE, SET_MSG_OK_SAVE, SET_MSG_TIMEOUT, SET_NO_BLUE, SET_NO_GREEN, SET_NO_RED, SET_NO_YELLOW, SET_REINCARNATION, SET_SPELL_ENTRY, SET_TIMER_GOING, SET_WOOD_COLLECTION_RADII, SPELL_AT_MARKER, SPELL_AT_THING, STATE_SET, TARGET_BLUE_DRUM_TOWERS, TARGET_BLUE_SHAMAN, TARGET_BLUE_SUPER_WARRIORS, TARGET_FIREWARRIORS, TARGET_GREEN_DRUM_TOWERS, TARGET_GREEN_SHAMAN, TARGET_GREEN_SUPER_WARRIORS, TARGET_MEDICINE_MAN, TARGET_RED_DRUM_TOWERS, TARGET_RED_SHAMAN, TARGET_RED_SUPER_WARRIORS, TARGET_S_WARRIOR, TARGET_SHAMAN, TARGET_WARRIORS, TARGET_YELLOW_DRUM_TOWERS, TARGET_YELLOW_SHAMAN, TARGET_YELLOW_SUPER_WARRIORS, THING_COUNT_IN_AREA, TRAIN_PEOPLE_NOW, TRIGGER_LEVEL_LOST, TRIGGER_LEVEL_WON, TRIGGER_THING, TURN_PUSH_OFF, TURN_PUSH_ON, DO_TRIGGER

   Plus all the read-access functions for INT_* values:
   MY_NUM_PEOPLE, BLUE_PEOPLE, RED_PEOPLE, YELLOW_PEOPLE, GREEN_PEOPLE, MY_NUM_KILLED_BY_BLUE, etc. (these are the ~300 INT_ internal variables that return game state)

6. Total registered items breakdown:
   - ~168 function stubs (panic on call)
   - ~299 PopScript module constants (integer values)
   - ~3170 Defines constants (integer values)
   = ~3637 total Lua globals
  </action>
  <verify>
    <automated>cd /Users/adrien/Development/rust/pop3 && cargo test engine::ai::constants::tests -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/ai/constants.rs contains `pub fn register_constants`
    - src/engine/ai/constants.rs contains `INT_BLUE` set to 0
    - src/engine/ai/constants.rs contains `INT_BRAVE` set to 2
    - src/engine/ai/constants.rs contains `ATTACK_NORMAL`
    - src/engine/ai/constants.rs contains panic message "not yet implemented" for stub functions
    - cargo test engine::ai::constants::tests passes
    - cargo build succeeds
  </acceptance_criteria>
  <done>All 3469+ PopScript constants registered as Lua globals, 168 PopScript function stubs registered (panic on call per D-04), tests verify key constants accessible</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds with mlua/serde/bincode dependencies
- `cargo test` passes (all existing tests + new AI module tests)
- AiSystem can create a Lua VM and execute simple Lua expressions
- Constants are accessible from Lua: `lua.globals().get::<i32>("INT_BLUE")` returns 0
</verification>

<success_criteria>
- mlua 0.11, serde 1.x, bincode 2 in Cargo.toml and compiling
- src/engine/ai/ module exists with mod.rs and constants.rs
- AiSystem struct owns a Lua VM, implements AiTick
- 3469+ constants registered as Lua globals
- 168 PopScript function stubs registered (panic on call)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-and-campaign/05-01-SUMMARY.md`
</output>
